{{ define "tags" }}
{{/* Created on 24. 02. 2021 */}}
{{/* Time-stamp: <2021-03-10 20:22:25 krylon> */}}
<!DOCTYPE html>
<html>
  {{ template "head" . }}

  <body>
    {{ template "intro" . }}

    {{ template "tag_form" . }}

    <hr />

    <div class="container-fluid">
      <div class="row">
        <div class="col-auto align-self-start">
          <ul>
            {{ range .Tags }}
            {{ template "tag_view" . }}&nbsp;
            {{ end }}
          </ul>
        </div>

        <div id="item_div" class="col">
          <small>
            Put Items here
          </small>
        </div>
      </div>
    </div>

    {{ template "footer" }}
  </body>
</html>
{{ end }}
